/**
 * This stub is provided to make it straightforward to get started.
 */

export function twoFer(name:string):string {
    return `One for ${typeof name !== "undefined" ? name:"you"}, one for me.`
}
